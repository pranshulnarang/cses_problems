#include <bits/stdc++.h>
using namespace std;

int main(){
    ios::sync_with_stdio(0);
	cin.tie(0); cout.tie(0);
    long long n,m;
    cin>>n>>m;
    long long h[n];
    long long t[m];
    int i;
    for(i=0;i<n;i++)
    cin>>h[i];
    for(i=0;i<m;i++)
    cin>>t[i];
    sort(h,h+n);

}